# Optional: absolute API origin for production builds (enables calling Django
# from a different host; Django must allow CORS for that origin).
# Leave unset for local dev — Vite proxies /api to http://127.0.0.1:8000.
# VITE_API_BASE=http://127.0.0.1:8000
